<%= form_for(@instruction_set) do |f| %>
  <% if @instruction_set.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@instruction_set.errors.count, "error") %> prohibited this instruction_set from being saved:</h2>

      <ul>
      <% @instruction_set.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
