<%= form_for(@instruction) do |f| %>
  <% if @instruction.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@instruction.errors.count, "error") %> prohibited this instruction from being saved:</h2>

      <ul>
      <% @instruction.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

Select Instruction
  <%= collection_select(:instruction_type, :template_id, InstructionTemplate.all, :id, :name) %>

  <div class="field">
    <%= f.label :code %><br />
    <%= f.text_area :code %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
