<%= form_for(@instruction_template) do |f| %>
  <% if @instruction_template.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@instruction_template.errors.count, "error") %> prohibited this instruction_template from being saved:</h2>

      <ul>
      <% @instruction_template.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
